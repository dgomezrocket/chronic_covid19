{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build --prefix packages/shared-types && npm run build --prefix packages/api-client && npm run build --prefix apps/web",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}